
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import Tk, Canvas, Entry, Text, Button, PhotoImage, messagebox
from pymongo import MongoClient
import subprocess
import hashlib

CONNECTION_STRING = "mongodb+srv://codershubcode:yiEq1ICyfn1E6UsO@cluster0.jnw3vvs.mongodb.net/?retryWrites=true&w=majority"

OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"/Users/User/Desktop/tkdesigner-figma/login-signup-form/logout/build/assets/frame0")
# Local Database - subprocess.Popen(["mongod", "--dbpath",  "/Users/User/Desktop/tkdesigner-figma/login-signup-form/DB-Storage/Data",  "--logpath",  "/Users/User/Desktop/tkdesigner-figma/login-signup-form/DB-Storage/Logs/mongo.log",  "--logappend"])
mongo_client = MongoClient(CONNECTION_STRING)
db = mongo_client.users
logged_in_users_collection = db.logged_in_users
registered_users_collection = db.registered_users


def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)

def find_email_address_and_password_and_delete():
    find_users = logged_in_users_collection.find_many({})
    delete_all_users = registered_users_collection.delete_many({})
    if (find_users == None):
        messagebox.showerror("Error", "No user logged in.")
    else:
        delete_all_users()
        # Local Database - subprocess.run(["killall", "mongod"])
    exit()

window = Tk()

window.geometry("447x732")
window.configure(bg = "#97DEFF")


canvas = Canvas(
    window,
    bg = "#97DEFF",
    height = 732,
    width = 447,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

canvas.place(x = 0, y = 0)
button_image_1 = PhotoImage(
    file=relative_to_assets("button_1.png"))
button_1 = Button(
    image=button_image_1,
    borderwidth=0,
    highlightthickness=0,
    command=find_email_address_and_password_and_delete,
    relief="flat"
)
button_1.place(
    x=64.0,
    y=274.0,
    width=316.0,
    height=92.0
)

canvas.create_text(
    102.0,
    89.0,
    anchor="nw",
    text="Click the button",
    fill="#2E4F4F",
    font=("Inter", 32 * -1)
)

canvas.create_text(
    100.0,
    128.0,
    anchor="nw",
    text="below to log out",
    fill="#2E4F4F",
    font=("Inter", 32 * -1)
)
window.resizable(False, False)
window.mainloop()
